package oog.re;

// NOTE: MOOG does not have a base class called Heuristic. because heuristic objects are created only inside OOGRE.
// ArchDoc reads them in order to display them only.
public interface IHeuristic extends IOperation {
}
